#!/usr/bin/env python


######################TODO LIST############################


# SO this is finished but rolled back
#TODO The first thing we should do it make the store actually distributed and have the leader ask them to push to store
    # Tried to add commit_to_store() this is really ineffecient and is bringing down our performance by a lot maybe
    # we just commit right when we see the put?  -- This seems like the wrong thing to do though when looking over 2PC

    # have rolled back please see last line in <recieve_acks> function.

#TODO Next we need to add in timers and election processes

#TODO Add in full message log with time series so that we can reconcile partitions

#TODO Optimize


import sys, socket, select, time, json, random

# Your ID number
my_id = sys.argv[1]

# The ID numbers of all the other replicas
replica_ids = sys.argv[2:]

# Connect to the network. All messages to/from other replicas and clients will
# occur over this socket
sock = socket.socket(socket.AF_UNIX, socket.SOCK_SEQPACKET)
sock.connect(my_id)

last = 0
store = {}
replica_ids.append(my_id)
messageLog = {}
rollback_temp = None
quorum_cutoff = 1 #ms


def initial_election():
    ranks = {x: int(x, 16) for x in replica_ids}
    min_id = min(ranks.values())

    for replica in ranks:
        if ranks[replica] == min_id:
            return replica


def send_redirect(msg):
    redirect_msg = {"src": msg['dst'], "dst": msg["src"], "leader": LEADER_ID, "type": "redirect", "MID": msg['MID']}
    sock.send(json.dumps(redirect_msg))


def answer_get(msg):
    if msg['key'] in store:
        return_msg = {"src": LEADER_ID,
                      "dst": msg['src'],
                      "leader": msg['leader'],
                      "type": "ok",
                      "MID": msg['MID'],
                      "value": store[msg['key']]}
    else:
        return_msg = {"src": msg['dst'],
                      "dst": msg['src'],
                      "leader": msg['leader'],
                      "type": "fail",
                      "MID": msg['MID'],
                      "value": ""}
    sock.send(json.dumps(return_msg))


def answer_put(msg):
    # if leader, broadcast to followers
    global start_time, rollback_temp
    if msg["dst"] == LEADER_ID:
        messageLog[msg["MID"]] = set()
        to_followers = {"src": LEADER_ID, 'dst': "FFFF", "leader": LEADER_ID, "type": "put", "MID": msg['MID'], "origin": msg["src"], 'key': msg['key'], 'value': msg['value']}
        sock.send(json.dumps(to_followers))

    # replica answer put
    elif msg["src"] == LEADER_ID:
        # store
        if msg["key"] in store:
            rollback_temp = store[msg["key"]]

        store[msg["key"]] = msg['value']
        # If a put is received from the leader, send an ACK back to the leader
        to_leader = {"src": my_id, 'dst': LEADER_ID, "leader": LEADER_ID, "type": "ok", "MID": msg['MID'], "origin": msg["origin"], 'key': msg['key'], 'value': msg['value']}
        sock.send(json.dumps(to_leader))



def quorum_timeout(start_time):
    print(time.time() - start_time)
    return time.time() - start_time > quorum_cutoff


def quorum(mid):
    #print(mid)
    #print(replica_ids)
    return len(mid) >= len(replica_ids) // 2


def send_rollback(msg):
    for src in messageLog[msg["MID"]]:
        rollback_msg = {"src": LEADER_ID, "dst": src, "leader": LEADER_ID, "type": "rollback", "MID": msg['MID'], "key": msg['key']}
        sock.send(json.dumps(rollback_msg))

def answer_rollback(msg):
    print("rolling back")
    if rollback_temp is not None:
        store[msg["key"]] = rollback_temp
    else:
        store.pop(msg["key"])

def quorum_reached(msg):
    global start_time

    to_client = {"src": LEADER_ID, 'dst': msg["origin"], "leader": LEADER_ID, "type": "ok", "MID": msg['MID']}
    sock.send(json.dumps(to_client))
    messageLog.pop(msg["MID"])
    # This needs to be changed we should only commit to the store when we know all replicas have the change
    # We need to have this become a distributed commit
    # commit_to_store(msg)
    # Can rollback via next line
    store[msg["key"]] = msg['value']
    start_time = time.time()


def receive_acks(msg):
    #print(quorum_timeout(start_time))
    if msg["MID"] in messageLog.keys():
        messageLog[msg["MID"]].add(msg['src'])
        if quorum(messageLog[msg['MID']]):
            quorum_reached(msg)


    # # quorum timeout reached - do fail
    # fail_msg = {"src": msg['dst'], "dst": msg['origin'], "leader": LEADER_ID, "type": "fail", "MID": msg['MID']}
    # sock.send(json.dumps(fail_msg))
    #
    # #rollback
    # if msg["key"] in store:
    #     rollback_temp = store[msg["key"]]
    # send_rollback(msg)



# def commit_to_store(msg):
#     store[msg["key"]] = msg['value']
#     to_replicas = {"src": LEADER_ID, 'dst': 'FFFF', "leader": LEADER_ID, "type": "commit", "MID": msg['MID'], 'key': msg["key"], 'value': msg['value']}
#     sock.send(json.dumps(to_replicas))





# def wait_for_responses():
#     # Make a timeout
#     timer_start = time.time()
#     responses = {}
#
#     while time.time() - timer_start < .01:
#         ready = select.select([sock], [], [], 0.1)[0]
#         for s in ready:
#             msg_raw = s.recv(32768)
#
#             if len(msg_raw) == 0: continue
#
#             msg = json.loads(msg_raw)
#
#             if msg["src"] not in replica_ids: continue
#
#             responses[msg["src"]] = msg
#
#     #for k,v in responses.items():
#         #print("{} : {}".format(k,v))
#
#     #print("{} : {}".format(len(responses), len(replica_ids)))
#     return responses


#####################################################
#                   Main Function                   #
#####################################################

LEADER_ID = initial_election()
IS_LEADER = LEADER_ID == my_id
state = False
start_time = None

while True:
    ready = select.select([sock], [], [], 0.1)[0]

    if sock in ready:
        msg_raw = sock.recv(32768)

        if len(msg_raw) == 0: continue
        msg = json.loads(msg_raw)

        # if not is_leader and (current_ms() - last_hearbeat >= election_timeout):
        #     run_for_leader()
        #
        # if is_leader and (current_ms() - last_hearbeat > heartbeat_timeout):
        #     send_heartbeat()

        # handle get() and put() from clients
        # or (msg["src"] == LEADER_ID and msg["dst"] in replica_ids):
        if msg["dst"] == LEADER_ID:
            if msg['type'] == 'get':
                answer_get(msg)

            elif msg["type"] == 'put':
                answer_put(msg)
                start_time = time.time()

            if time.time() - start_time <= quorum_cutoff:
                if msg["type"] == "ok":
                    receive_acks(msg)
            else:
                send_rollback(msg)
                start_time = time.time()


        elif msg['src'] == LEADER_ID: #and msg['dst'] == 'FFFF':
            if msg["type"] == 'put':
                # reset hbtimer
                answer_put(msg)
            if msg["type"] == 'rollback':
                answer_rollback(msg)

        # Need new logic here... This way we can keep track of how many acks we have gotten
        else:
            #print(msg)
            send_redirect(msg)

    # clock = time.time()
    # if clock - last > 2:
    #     # Send a no-op message to a random peer every two seconds, just for fun
    #     # You definitely want to remove this from your implementation
    #     msg = {'src': my_id, 'dst': 'FFFF', 'leader': LEADER_ID, 'type': 'noop'}
    #     sock.send(json.dumps(msg))
    #     #print('%s sending a NOOP to %s' % (msg['src'], msg['dst']))
    #     last = clock
